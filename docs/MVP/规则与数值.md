# MVP 规则与数值

> 本文集中维护首版规则与参数。调用方法见[接口与方法](../开发者手册/接口与方法.md)，可调整事项见[待办事项](./待办事项.md)。

## 一、数值约定

智力、道德、政治、交际、体能为五项通用属性；识字、经义、文章、策论、文学为五项书生能力。两组数值与健康、疲劳、阅读进度均使用 0～100 的整数。金额以整数文保存，考试分数也使用整数；总学识允许小数且不封顶。

计算中使用 `BigDecimal`，整数结果按 `HALF_UP` 四舍五入。`clamp(下限, 上限, 数值)` 表示把数值限制在指定范围。实际收益等于最终值减原值，超出上限的部分不算收益。

规则常量见 [GameRuleConstant.java](../../src/main/java/mvp/engine/GameRuleConstant.java)，书籍参数见[资源配置](../开发者手册/资源配置.md)。以下都是首版参数，可以继续调整。

## 二、开局与时间

| 项目 | 初始值 |
| --- | --- |
| 出生年／开局年 | 1541／1547 |
| 开局时间 | 六岁，正月第一回合，累计回合 0 |
| 五项通用属性 | 各 20 |
| 健康／疲劳 | 75／0 |
| 识字／其余书生能力 | 5／各 0 |
| 人物钱包 | 2000 文 |
| 家庭财富 | 固定 100000 文；规则允许范围为 10000～1000000 文 |

家庭财富只表示出生背景，与钱包分别保存；交易改变钱包。玩家与 NPC 共用人物、钱包、书生档案和背包结构。开局创建固定 NPC、补齐公共教材定义，背包起初为空。

童年背景通过独立接口生成并缓存，提示词要求 250～500 字，程序拒绝空白或超过 1000 个 Unicode 字符的结果。叙事只展开六岁入学前的既定背景，不回写初始属性、财富和时间；开局接口本身不调用模型。

每月 4 回合，每年 12 个月，年龄按当前游戏年份减人物出生年份计算，跨年增加。玩家与 NPC 共用时间线，但各自使用自己的出生年。读书、练习、休息和自由行动消耗一回合；查看、进入场景、获取物品和普通对话不消耗回合。场景随行动传入，不单独保存当前场景或移动过程。

| 年龄 | 考试 | 累计回合 | 通过线 |
| --- | --- | --- | --- |
| 8 | 蒙学阶段考 | 96 | 35 |
| 12 | 经义阶段考 | 288 | 45 |
| 15 | 县试预考 | 432 | 52 |
| 16 | 县试 | 480 | 60 |

累计回合包括 NPC 行动及强制休养。上表年龄指玩家年龄；到达节点时先完成本回合，再用玩家当前属性、书生能力和学识准备考试。只有玩家考试结算能恢复求学或令存档进入 `COMPLETED`；NPC 考试结算不切换整局阶段。

## 三、教材与价格

| 提供者 | 书籍 | 品质 | 价格／文 | 读满学识 |
| --- | --- | --- | --- | --- |
| 先生 | 《三字经》 | 普通·白色 | 0 | 20 |
| 先生 | 《千字文》 | 普通·白色 | 0 | 20 |
| 先生 | 《论语》 | 普通·白色 | 0 | 40 |
| 先生 | 《孟子》 | 优良·绿色 | 0 | 60 |
| 先生 | 《大学》 | 优良·绿色 | 0 | 50 |
| 先生 | 《诗经》 | 优良·绿色 | 0 | 45 |
| 先生 | 《名家文钞》 | 稀有·蓝色 | 0 | 70 |
| 书商 | 《经世策论集》 | 稀有·蓝色 | 400 | 100 |
| 书商 | 《八股破题精义》 | 稀有·蓝色 | 600 | 120 |
| 书商 | 《GPT-1的权重》 | 传说·金色 | 1000 | 180 |

所有装备共用品质：普通（白）、优良（绿）、稀有（蓝）、史诗（紫）、传说（金）。品质用于分档展示，阅读速度由人物决定；能力收益权重、疲劳消耗和总学识按各书配置。

《GPT-1的权重》对应 *WEIGHTS — The Complete Parameters of GPT-1*，原项目把约 1.17 亿个参数印成 80 卷，首卷附纸笔计算说明。游戏把整套作为一件彩蛋物品；原书是模型参数集，游戏中的学识和能力加成属于玩法设定。[出版页](https://weights-press.netlify.app/)、[完整卷目](https://the-open-accelerator.com/GPT-1/index.html)

## 四、阅读与学识

### 阅读条件

必须处于求学阶段，背包有书，并同时满足配置要求的年龄、通用属性、领域能力和前置书籍进度。`growthDomainCode` 只表示收益所属领域，不限制人物身份。

所有书籍统一以进度 100 为读满。同一本书按人物分别记录进度。没有阅读记录时按 0 显示，第一次读书时创建；多本副本不重复增加学识，失去书籍不清空已学进度。

### 常规计算

```text
智力系数 = 0.75 + 0.50 × 智力 / 100
身体系数 = clamp(0.55, 1.20, 0.65 + 健康×0.003 + 体能×0.002 - 疲劳×0.003)
学习量 = 13 × 智力系数 × 身体系数
普通进度增量 = min(剩余进度, max(0, round(学习量)))
综合能力 = 五项书生能力按本书百分比权重求和
递减系数 = clamp(0.20, 1, 1 - 综合能力 / 120)
学习池 = (0.50 + 0.20 × 学习量) × 递减系数 × 温习系数
单项能力增量 = round(学习池 × 本书该项权重 / 100)
```

基础阅读速度统一为每回合 13 点，由 `GameRuleConstant.READING_PROGRESS_PER_TURN` 维护；开局属性20、健康75、疲劳0时，普通骰点下实际推进10点。同一人物状态下，各书的常规阅读速度一致。行动开始时未读满，温习系数为 1；已经读满则为 0.35。各项能力分别取整，小于 0.5 的收益会变成 0，暂不累计余数。

### 阅读骰点

1D100 表示产生一个 1～100 的随机整数，由业务层在有效阅读时生成，作为计算方法的参数。

| 骰点 | 结果 |
| --- | --- |
| 1 | 进度和能力收益为 0 |
| 2～99 | 按常规公式计算 |
| 100 | 直接补满进度，能力仍只按本回合正常收益计算 |

三种结果都消耗本回合，并计算书籍疲劳。实际骰点与结果一并保存，重传同一请求不会重投。首次读满保存 `BOOK_MASTERED` 人生节点。

### 以身入局读书

允许此方式的科举类书籍可先由 AI 提出一道具体的体会题，玩家亲自输入自己的理解与理由，AI 返回0至100分及点评。初始七本教材、《经世策论集》《八股破题精义》开启，《GPT-1的权重》关闭；由书籍的 `playerReadingEnabled` 决定。

| 得分 | 评分对应进度 |
| --- | --- |
| 0 | 10 |
| 50 | 50 |
| 80 | 74 |
| 90 | 82 |
| 100 | 90 |

AI评分先限幅到0～100并四舍五入取整，再按 `进度 = round(10 + 80 × 评分 / 100)` 线性换算，四舍五入得到10～90点。实际收益不超过剩余进度，因此接近读完时可以少于10点。这些是本次增加的进度，不是读后的总进度；从0开始满分也只能到90。人物属性和身体状态不再改变这部分评分进度。

出题和评阅采用宽松标准，以具备北京市重点高中语文受教育水平、语文高考约110分（150分制）的玩家认真作答即可满分为设计目标。题面提供必要背景；回应核心问题、理解基本准确、用自己的话说明一个合理理由或具体做法，即可给100分。无需专业经学知识、独到见解、引用经文或长篇文章；朴素白话、个别错字及不影响理解的小疏漏不扣分，满分点评不强行挑错。评分仍由AI根据实际体会判断，不按玩家自报的学历或成绩给分。

目标节奏是一次普通阅读约10点，再用一次满分体会完成余下90点。状态较差、普通骰点失常或评分较低时可能需要更多回合。以身入局不额外掷阅读骰子，评分奖励只增加进度，能力与疲劳仍按一次普通读书结算。

出题不耗回合，同一玩家、同一本书、同一回合复用原题。交卷成功消耗一个正常读书回合，同一题同一原文重传返回原结果，换原文不能再次结算。出题后若先做了推进回合的行动，要重新出题；AI失败不改变进度和时间。已读满的书使用普通阅读温习。

### 学识计算

```text
p = 0：贡献0
1 ≤ p ≤ 99：贡献 = 单书满学识 × 0.6 × p / 100
p = 100：贡献 = 单书满学识
总学识 = 全部不同书籍的贡献之和
考试知识分K = clamp(0, 100, round(总学识 / 2))
```

`p` 是阅读进度。“不同书籍”按装备定义 ID 区分。学识保留小数，进入考试换算时才取整；书籍内容介绍不按阅读档位拆分。

## 五、练习、休息与健康

练习文章的原始增长为 `2.40 × 智力系数 × 身体系数 × clamp(0.25, 1, 1 - 文章/120)`，取整后加入文章能力，基础疲劳为 4。

休息恢复疲劳 `18 + round(体能×0.10)`，恢复健康 `3 + 体能/25的整数商 + (原疲劳≥70 ? 1 : 0)`，结果限制在 0～100。

### AI 带来的成长

自由行动及整场对话结束时，AI 给出变化量，再由程序限制驱动量、取整并计算实际结果：

| 项目 | 每次结算的驱动量范围 |
| --- | --- |
| 通用属性 | 每项 ±3 |
| 书生能力 | 每项 ±5 |
| 疲劳 | ±20 |
| 健康 | ±10 |

缺省值为 0，不保证每次互动都有成长。上表限制 AI 直接给出的驱动量，最终健康变化还会叠加过劳伤害及重病处理。固定读书、练习主要提升书生能力。

### 疲劳和重病

行动疲劳为 `round(基础疲劳 × (1.15 - 体能/200) × (1 + max(0,60-健康)/100))`；基础疲劳大于 0 时至少增加 1。仅当本次疲劳变化量取整后大于 0，才计算过劳伤害：行动后疲劳超过 80，每超过 10 点（向上取整）损失 1 健康。自由行动或对话结算的疲劳变化量为 0 或负数时，不追加过劳伤害；AI 直接给出的健康变化仍正常结算。

健康降到 0 且尚未重病时，通用属性各减 2，并进入 4 个休养回合。每回合推进日历、疲劳减 20，最后一回合恢复健康至 40。属性只在进入重病时扣一次。

休养遇到玩家考试先暂停，剩余回合保留。玩家在阶段考试后继续休养；NPC 则在下次收到自己的固定行动请求时继续休养，不由玩家考试代为调度。玩家县试结束后不再推进。读书、自由行动或对话结算都可能触发休养；已在重病中的固定行动请求只处理休养。首版不设死亡或后台倒计时。

## 六、考试计算

玩家节点考试开始时，用节点回合完成后的玩家数据计算基础分 `B`：通用属性、书生能力和考试知识分按题目权重相加，四舍五入后限制在 0～100；权重合计为 1.00。

身体修正 `R` 和普通运气修正 `L` 为：

```text
R = round(clamp(-8, 4,
    (健康-70)×0.06 - max(0,疲劳-20)×0.08 + (体能-50)×0.03))
x = (骰点 - 50.5) / 49.5
普通运气修正L = round(25 × x³)
```

题目、通过线、`B`、`R`、总学识、骰点及 `L` 一起保存。之后即使人物状态或参数改变，也不重算这次考试的数据。

| 骰点 | 系统代行结果 |
| --- | --- |
| 1 | 0 分，必定失败 |
| 2～99 | `clamp(0,100,B+R+L)` |
| 100 | 100 分，必定通过 |

玩家作答使用 `clamp(0,100,B+R+L+round(clamp(-10,10,M)))`；`M` 是 AI 按题目及评分点给出的答案内容加减分，最终最多加 10 分或减 10 分。程序先限幅再四舍五入，避免异常大数转为整数时溢出；骰点 1 和 100 的极端结果仍优先。内容评价不能替代角色本身的能力、身体状态和已保存骰点。

例如 `B+R+L=50`：内容修正为 `+8` 时得 58 分，为 `-6` 时得 44 分；即使 AI 返回 `+30`，程序也只加 10 分。模型只评价答案，最终分数与是否通过由程序决定。

角色思路按 `B+R` 的 35、60、80 分界分为四档。首次请求时让 AI 依据角色水平和已读书籍生成提示，并保存至该考试；再次请求复用，不重投骰点或透露最终成绩。提示是思路，不是完整答案。

系统代行先计算最终成绩，再生成相符的完整答卷和总结。玩家作答先取得内容评价，再由程序计分，最后生成结果总结；模型不能回写分数或通过状态。生成失败时保留待考状态，不保存部分答卷或成绩；先前已成功保存的思路不受影响。

重复结算同一考试返回原成绩，不重复推进阶段；不同答案或不同作答方式不能覆盖已完成记录。考试本身不消耗回合，但考试后可能继续剩余休养回合。旧版已经完成的考试保持原结果，不重新评价或补写 AI 内容。

## 七、物品获取与对话

### 领取和购买

七本学校教材由先生免费提供，三本付费书由书商提供，均须实际获取才入包。单次数量为 1～100，当前不限制免费领取次数或供应库存，也不扣供应者的背包。

价格和提供者由数据库定义决定。扣款、提供者收款、背包记录和请求结果须在同一事务内完成。每次获取分别记录，同种物品在背包中合计数量。免费获取不扣钱，普通获取不消耗回合；自由行动中附带获取时，整次行动仍消耗回合。

### 自由行动与人物对话

程序先读取当时的数据，再调用 AI；模型调用放在数据库事务之外。得到结果后核对人物和回合是否变化，再执行交易、保存属性与时间；过期结果不覆盖新状态。

自由行动与对话共用游戏日期、行动人物的真实年龄和书生能力、玩家初始家庭背景、场景、书目及 NPC 事实。家庭背景不视为 NPC 的个人背景，也不能代替人物钱包。具体输入字段见[接口与方法](../开发者手册/接口与方法.md#七存档与行动)。

普通对话往返可以完成本轮交易，不结算属性。玩家手动结束或 AI 决定结束时，整场只结算一次属性。手动结束只总结历史，不重做旧交易；结束失败也不撤销此前已成功的交易。

一次行动者输入与对方回应算一轮，`version` 记录已完成轮数。每场最多 5 轮，模型接收 `dialogueRound` 和 `maxDialogueRounds=5`，通过 `endDialogue=true` 表示结束；语义完整时可以提前结束，第 5 轮必须结束。若模型第 5 轮仍返回 `false`，程序拒绝本轮结果并返回 502，可原样重试，不替模型强行结算。

AI 说“送了书”不等于已入包，必须以程序实际执行的交易为准。行动或对话调用失败时返回错误，提示检查网络或模型账户余额，不自动重试。

### 跨场记忆

记忆来源限定为已提交的 `END_DIALOGUE` 和 `lifeMilestone` 事件。结束对话依据完整回执中的确认发言和实际交易为双方生成摘要；重要经历只为事件记录中的真实参与者生成摘要，不使用内部 `AI_ACTION` 作为独立记忆来源。

每条摘要不超过 1000 个 Unicode 字符。自由行动读取行动者自己的记忆，对话读取回应者自己的记忆，最多选取 8 条；压缩摘要、人物/事件标识等组成的整体记忆 JSON 不超过 5000 个 Unicode 字符，不把全量历史原文带入后续上下文。优先选择与当前对象有关的近期记忆，其余使用本人的近期记忆。

正文写入项目运行目录下的 `memory/<saveId>/<memoryId>.json`，数据库只保留记忆 ID、归属人物、来源事件、相关人物、场景、相关物品及回合。记忆在事件提交后生成；失败不影响已完成的行动，后续相关调用少量补齐。

## 八、重复请求与随机结果

固定行动、获取、自由行动和对话使用 `requestId`。同一存档、同一编号、相同参数重传时返回原结果；同一编号换参数则拒绝。新操作使用新编号。

固定行动还检查 `expectedTurnNumber`，对话检查 `expectedVersion`，防止旧页面覆盖新数据。请求结果记录与人生节点分别展示，普通操作不会刷满人生事件列表。

计算类不访问数据库、不调用模型、不自行掷骰；相同输入和骰点得到相同结果。实际游戏的随机数在业务层生成并保存，UUID 主键不属于玩法随机。[随机种子](../后续设计/随机种子.md)留待后续实现。
